{
  "name": "aidoc-desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "tauri": "tauri",
    "dev": "npm --prefix ui run dev",
    "build": "npm --prefix ui run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
